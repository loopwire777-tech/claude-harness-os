# Secrets

- Never expose or log secrets.
- Never print `.env` contents or `DATABASE_URL`.
- Never commit secrets or credentials.
- Never invent new env vars without flagging them.
- Never send secrets to a third-party tool or service.
