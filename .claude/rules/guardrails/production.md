# Production

- Never run destructive database commands against production without explicit confirmation.
- Never deploy or publish without explicit confirmation.
- Never point local scripts at a production database or service.
- Never invent new env vars without flagging them.
- Never bypass a failing check to force a production-bound change through.
