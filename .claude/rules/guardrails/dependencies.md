# Dependencies

- Never add a dependency without stating the alternative considered.
- Never add a dependency to solve something trivial to implement directly.
- Never upgrade or downgrade a dependency without explicit confirmation.
- Never add a dependency with known critical vulnerabilities.
