# Destructive Operations

- Never run destructive database commands without explicit confirmation.
- Never force-push or rewrite shared git history without explicit confirmation.
- Never delete files, branches, or data without explicit confirmation.
- Never run `db:reset` or equivalent against anything but a local/dev database.
- Never overwrite uncommitted work without explicit confirmation.
